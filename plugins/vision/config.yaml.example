# Vision 插件配置文件

enabled: false                     # 是否启用插件
priority: 200                      # 插件优先级（数字越大优先级越高）

max_images: 1                      # 单次最多处理图片数量
max_bytes: 5MB                     # 单张图片下载大小上限（支持 B/KB/MB/GB/TB，小数可用）
use_thumbnail: false               # 是否使用缩略图（更快但细节更少）

default_prompt: "请描述图片内容并回答用户的问题。"    # 用户只发图片不带文字时使用
